# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri build output
tauri-src/target
tauri-src/gen/schemas

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Symlink to tauri-src/target/release/bundle
build

# Staged ffmpeg/ffprobe sidecars (see stage-ffmpeg.sh)
tauri-src/binaries

# An LGPL ffmpeg built inside the repo (docs/ffmpeg-build.md suggests a
# sibling directory, but a checkout-local build must not get committed)
ffmpeg-lgpl/

# Secrets and machine-local config (signing identities, notarization profiles)
.env
.env.*

# TypeScript incremental build state
*.tsbuildinfo

docs

# Claude Code's working directory: agent notes, product brief, local docs and
# session-local permissions. Local only — CLAUDE.md was tracked until Aug 2026,
# and a gitignore entry alone would not have untracked it.
.claude

# Mock-device tooling, local only
scripts/make-mock-devices.sh
